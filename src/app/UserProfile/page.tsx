import React from 'react';
import UserProfile from './UserProfile';  // Corrected import path

const Page: React.FC = () => {
  return (
    <div>
      <UserProfile />
    </div>
  );
};

export default Page;
